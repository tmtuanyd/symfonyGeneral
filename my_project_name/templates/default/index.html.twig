{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% set users = [{name: 'John'}, {name: 'Susan'}] %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name|upper }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/Users/trantuan/Desktop/Tuan/Learn/Symfony/my_project_name/src/Controller/DefaultController.php'|file_link(0) }}">src/Controller/DefaultController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/Users/trantuan/Desktop/Tuan/Learn/Symfony/my_project_name/templates/default/index.html.twig'|file_link(0) }}">templates/default/index.html.twig</a></code></li>
    </ul>
    <ul>
        {% for user in users %}
{#        <li>{{ user.name }} won {{ gifts[loop.index0] }}</li>#}
            <li>{{ user.name }}</li>
        {% endfor %}
    </ul>
    {% for message in app.flashes('notice') %}
    <div class="flash-notice">
        {{ message }}
    </div>
    {% endfor %}
    {{ include('includes/subview.html.twig', {'users': users}) }}
    {% for i in 1..10 %}
        <div class="{{ cycle(['even', 'odd'], i) }}">
            {{ i }}
        </div>
    {% endfor %}
    {{ path('home') }} <br>
    {{ url('home') }} <br>
    <p>the google tracking code is : {{ ga_code }}</p>
{#    {{ app.user }}#}
{#    {{ app.request.get('param') }}#}
{#    {{ app.evironment }}#}
    {{ render(controller(
        'App\\Controller\\DefaultController::mostPopularPosts',
        {'number':5}
    )) }}
</div>
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('build/js/custom.js') }}"></script>

    </script>
{% endblock %}

